# Database Configuration
POSTGRES_USER=memo_user
POSTGRES_PASSWORD=phoenix
POSTGRES_DB=memo_app
POSTGRES_HOST=localhost
POSTGRES_PORT=5433

# Database URL for FastAPI (Local Development)
# Note: When running locally, use localhost:5433. When running in Docker, use postgres:5432.
DATABASE_URL=postgresql+asyncpg://memo_user:phoenix@localhost:5433/memo_app

# Redis Configuration
REDIS_URL=redis://redis:6379

# Kafka Configuration
KAFKA_BOOTSTRAP_SERVERS=localhost:9092

# Node Environment
NODE_ENV=development
HOST=0.0.0.0
PORT=3000

# Grafana Configuration
GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=admin
